# Docker example: fetch depenencies to docker layer

This example shows how to build cargo-lock-fetch from source in a builder stage of a docker-based
rust project.

## Building

Run:

```sh
docker compose run --build example
```
